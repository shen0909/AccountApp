<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AccountApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AccountApp" parent="Base.Theme.AccountApp" />
    <style name="floatActionButton">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">100dp</item>
    </style>

    <style name="keyBroadStyle">
        <item name="android:width">0dp</item>
        <item name="android:height">50dp</item>
        <item name="android:textSize">20dp</item>
        <item name="android:background">@drawable/selec_tab</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:onClick">keyboardClick</item>
    </style>

    <style name="keyBroadTextStyle" parent="keyBroadStyle">
        <item name="android:textSize">16dp</item>
    </style>

</resources>